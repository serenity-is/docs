[serenity-is/corelib](../README.md) / registerEnum

# Function: registerEnum()

> **registerEnum**(`enumType`, `name`, `enumKey?`): `void`

Defined in: [src/base/system.ts:259](https://github.com/serenity-is/serenity/blob/master/packages/corelib/src/base/system.ts#L259)

Register an enum with the type system.

## Parameters

### enumType

`any`

Enum type to register

### name

`string`

Name to register the enum under

### enumKey?

`string`

Optional key to use for the enum

## Returns

`void`
