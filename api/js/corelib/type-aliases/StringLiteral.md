[@serenity-is/corelib](../README.md) / StringLiteral

# Type Alias: StringLiteral\<T\>

> **StringLiteral**\<`T`\> = `T` *extends* `string` ? `string` *extends* `T` ? `never` : `T` : `never`

Defined in: [src/base/system-internal.ts:24](https://github.com/serenity-is/serenity/blob/master/packages/corelib/src/base/system-internal.ts#L24)

## Type Parameters

### T

`T`
