[serenity-is/corelib](../../../../README.md) / [UploadHelper](../README.md) / colorBox

# ~~Variable: colorBox()~~

> `const` **colorBox**: (`link`) => `void` = `lightbox`

Defined in: [src/ui/helpers/uploadhelper.tsx:198](https://github.com/serenity-is/serenity/blob/master/packages/corelib/src/ui/helpers/uploadhelper.tsx#L198)

Creates a lightbox for a single upload thumbnail anchor element.
It uses one of glightbox, simplelightbox or colorbox if available.
Override this function to use a different lightbox library.

## Parameters

### link

`HTMLElement` | `ArrayLike`\<`HTMLElement`\>

## Returns

`void`

## Deprecated

use lightbox
