[@serenity-is/sleekgrid](../README.md) / addCssClass

# Function: addCssClass()

> **addCssClass**(`el`, `cls`): `void`

Defined in: [src/core/util.tsx:8](https://github.com/serenity-is/Serenity/blob/master/packages/sleekgrid/src/core/util.tsx#L8)

Adds one or more CSS classes to an element, supporting space-separated lists.

## Parameters

### el

`Element`

Target element.

### cls

`string`

Class name or space-separated class list to add. No-op when empty/null.

## Returns

`void`
