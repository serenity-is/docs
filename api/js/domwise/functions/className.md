[@serenity-is/domwise](../README.md) / className

# Function: className()

> **className**(`value`): `string`

Defined in: [src/class-name.ts:7](https://github.com/serenity-is/serenity/blob/master/packages/domwise/src/class-name.ts#L7)

Convert a `value` to a className string.
`value` can be a string, an array or a `Dictionary<boolean>`.

## Parameters

### value

`any`

## Returns

`string`
