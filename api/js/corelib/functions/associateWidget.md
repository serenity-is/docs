[@serenity-is/corelib](../README.md) / associateWidget

# Function: associateWidget()

> **associateWidget**(`widget`): `void`

Defined in: [src/ui/widgets/widgetutils.ts:20](https://github.com/serenity-is/serenity/blob/master/packages/corelib/src/ui/widgets/widgetutils.ts#L20)

Associates a widget with its DOM node so it can later be retrieved via
[tryGetWidget](tryGetWidget.md) or [getWidgetFrom](getWidgetFrom.md).

## Parameters

### widget

The widget to associate.

#### domNode

`HTMLElement`

## Returns

`void`
