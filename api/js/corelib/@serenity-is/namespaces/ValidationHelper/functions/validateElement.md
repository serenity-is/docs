[@serenity-is/corelib](../../../../README.md) / [ValidationHelper](../README.md) / validateElement

# Function: validateElement()

> **validateElement**(`elem`): `void`

Defined in: [src/compat/validation.ts:161](https://github.com/serenity-is/serenity/blob/master/packages/corelib/src/compat/validation.ts#L161)

Validates a single element using its associated [Validator](../../../../classes/Validator.md).
No-ops if no validator is attached to the element's form.

## Parameters

### elem

The element (or array-like wrapper) to validate.

`HTMLElement` | `ArrayLike`\<`HTMLElement`\>

## Returns

`void`
